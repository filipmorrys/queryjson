# QUERY JSON

Esta utilidad sirve para hacer queryes sobre un fichero JSON inicial de forma que se puedan filtrar aquellos objetos que se necesiten. El JSON de entrada debería 
ser un array de objetos y debe guardarse en la ruta:
```
./input/input.json
```

El resultado obtenido se guardará en el fichero:
```
./output/output.json
```